# This rule is not used by the project intel-gpu-base-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over intel.com.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: intel-gpu-base-operator
    app.kubernetes.io/managed-by: kustomize
  name: gpurecoveryplan-admin-role
rules:
- apiGroups:
  - intel.com
  resources:
  - gpurecoveryplans
  verbs:
  - '*'
- apiGroups:
  - intel.com
  resources:
  - gpurecoveryplans/status
  verbs:
  - get
