# This rule is not used by the project intel-gpu-base-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to intel.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: intel-gpu-base-operator
    app.kubernetes.io/managed-by: kustomize
  name: gpurecoveryplan-viewer-role
rules:
- apiGroups:
  - intel.com
  resources:
  - gpurecoveryplans
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - intel.com
  resources:
  - gpurecoveryplans/status
  verbs:
  - get
