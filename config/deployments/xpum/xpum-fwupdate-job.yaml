# Copyright (C) 2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0
# @file xpum-fwupdate-job.yaml
#
---
apiVersion: batch/v1
kind: Job
metadata:
  name: intel-fwupdate-job
spec:
  podFailurePolicy:
    rules:
    - action: FailJob
      onExitCodes:
        containerName: updater
        operator: NotIn
        values: [0]
    - action: FailJob
      onPodConditions:
      - type: ConfigIssue
  # A firmware update that has not finished in ten minutes is not going to. Fail the Job rather
  # than leave the node tainted and the CR parked in "updating" indefinitely.
  activeDeadlineSeconds: 600
  template:
    spec:
      volumes:
      - name: update
        emptyDir:
          sizeLimit: 64Mi
      # The update script resolves the MEI device of the target GPU through
      # /sys/bus/pci/devices/<bdf>/, so the updater needs the host's sysfs.
      - name: host-sys
        hostPath:
          path: /sys
          type: Directory
      automountServiceAccountToken: false
      initContainers:
      - name: fw-copy
        image: ubuntu:24.04 # replaced by the operator
        imagePullPolicy: IfNotPresent
        command: [ "/bin/sh", "-c" ]
        args:
          - "cp /fwupdate/* /update/"
        volumeMounts:
        - name: update
          mountPath: /update
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ "ALL" ]
          seccompProfile:
            type: RuntimeDefault
      containers:
      - name: updater
        image: intel/xpumanager:v1.2.27 # replaced by the operator
        imagePullPolicy: IfNotPresent
        command: [ "/bin/sh", "-c" ]
        args:
          - "sleep 20 && sleep $RANDOM"
        resources:
          requests:
            cpu: 50m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 64Mi
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: true
          runAsUser: 0
          privileged: true
          seccompProfile:
            type: RuntimeDefault
          capabilities:
            drop: [ "ALL" ]
        volumeMounts:
        - name: update
          mountPath: /update
        - name: host-sys
          mountPath: /sys
          readOnly: false
      imagePullSecrets:
      restartPolicy: Never

